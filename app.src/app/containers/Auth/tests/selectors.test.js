// import { fromJS } from 'immutable';
// import { makeSelectAuthDomain } from '../selectors';

// const selector = makeSelectAuthDomain();

describe.skip('makeSelectAuthDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
