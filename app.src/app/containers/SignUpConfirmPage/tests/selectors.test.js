// import { fromJS } from 'immutable';
// import { makeSelectSignUpConfirmPageDomain } from '../selectors';

// const selector = makeSelectSignUpConfirmPageDomain();

describe.skip('makeSelectSignUpConfirmPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
